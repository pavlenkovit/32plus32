import React from 'react';
import { NextPage } from 'next';

import VideoPreview from '../components/VideoPreview';

const WodPage: NextPage = () => {
  return <VideoPreview />;
};

export default WodPage;
